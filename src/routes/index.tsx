import { BrowserRouter, Routes } from 'react-router-dom';

export function AppRoutes() {
  return (
    <BrowserRouter>
        <Routes>
          <div>Route</div>
        </Routes>
    </BrowserRouter>
  );
}
